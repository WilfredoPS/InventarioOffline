name: inventario_flutter
description: "Sistema offline-first de gestión de inventario"
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  
  # Database local (offline-first)
  isar: ^3.1.0+1
  isar_flutter_libs: ^3.1.0+1
  path_provider: ^2.1.1
  
  # Backend y sincronización
  supabase_flutter: ^2.3.4
  
  # State management
  provider: ^6.1.1
  
  # Utilidades
  intl: ^0.19.0
  uuid: ^4.2.2
  shared_preferences: ^2.2.2
  connectivity_plus: ^5.0.2
  
  # UI y gráficos
  fl_chart: ^0.66.0
  google_fonts: ^6.1.0
  flutter_slidable: ^3.0.1
  
  # Manejo de imágenes
  image_picker: ^1.0.4
  cached_network_image: ^3.3.0
  image: ^4.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  
  # Code generation para Isar
  isar_generator: ^3.1.0+1
  build_runner: ^2.4.7

flutter:
  uses-material-design: true
  
  assets:
    - assets/images/